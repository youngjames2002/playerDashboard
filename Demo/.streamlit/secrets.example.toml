# Example secrets file.
#
# Copy this file to .streamlit/secrets.toml (same folder) and fill in your
# own start.gg API key — create one at https://start.gg/admin/profile/developer
# The real secrets.toml is gitignored and must never be committed.

STARTGG_TOKEN = "your-startgg-api-key-here"
